# Render Blueprint — https://render.com/docs/blueprint-spec
services:
  - type: web
    name: deepshield
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "7860"
      - key: FLASK_DEBUG
        value: "0"
